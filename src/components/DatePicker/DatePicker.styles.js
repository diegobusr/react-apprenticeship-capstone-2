import styled from 'styled-components';

export const DateDiv = styled.div`
  display: flex;

  span {
    color: white;
    text-shadow: 3px 3px 5px #000000;
  }

  input {
    font-size: 1em;
    margin: 0 5px 0 5px;
  }

  button {
    width: 60px;
    font-size: 0.7em;
  }
`;
